import hider, seeker, terminal

def main():
    #starting this train wreck up
    baddie = hider.hider()
    player = seeker.seeker()
    inandout = terminal.terminal()
    playing = True
    #Great let's start this game.
    inandout.print_response("Welcome to Hide and Seek")
    inandout.print_response("Catch the killer before he can run away")
    while playing == True:
        
    


if name =="__main__":
    main()